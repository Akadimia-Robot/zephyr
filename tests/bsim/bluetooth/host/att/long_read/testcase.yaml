tests:
  bluetooth.host.att.long_read:
    build_only: true
    tags:
      - bluetooth
    platform_allow:
      - nrf52_bsim
    harness: bsim
    harness_config:
      bsim_exe_name: tests_bsim_bluetooth_host_att_long_read_prj_conf
