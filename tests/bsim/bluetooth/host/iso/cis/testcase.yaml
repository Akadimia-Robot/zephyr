tests:
  bluetooth.host.iso.cis:
    build_only: true
    tags:
      - bluetooth
    platform_allow:
      - nrf52_bsim
    harness: bsim
    harness_config:
      bsim_exe_name: tests_bsim_bluetooth_host_iso_cis_prj_conf
