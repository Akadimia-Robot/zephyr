tests:
  bluetooth.host.gatt.notify:
    build_only: true
    tags:
      - bluetooth
    platform_allow:
      - nrf52_bsim
    harness: bsim
    harness_config:
      bsim_exe_name: tests_bsim_bluetooth_host_gatt_notify_prj_conf
